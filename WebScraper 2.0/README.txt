Make sure you have a gitbash installed for VS code

open folder in VS code

open new gitbash terminal

type "npm install puppeteer"

type "npm fund"

this will installed all the modules needed (for either windows or macOS)

type "index.js" to run the program